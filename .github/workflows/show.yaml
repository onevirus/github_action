name: Print secrets

on:
  push:
    branches:
      - main

jobs:
  show:
    steps:
    - name: Checkout
      run: |
        echo "${{ secrets.ABCD }}"
        echo "ABCDEF"
        echo "${{secrets.ABCD}}" | sed 's/./&\a/'
        echo "${{ secrets.ECHO}}"
